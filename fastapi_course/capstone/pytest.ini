[pytest]
# where the tests live
testpaths = tests
# put the project folder on the import path, so tests can `import app`
pythonpath = .
filterwarnings =
    # comes from inside the starlette library, not this project
    ignore:The anyio.abc.BlockingPortal alias is deprecated
